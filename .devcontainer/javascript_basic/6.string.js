let name= "hamza";
let email = "hamza.com";


//methods of strings

console.log(name.length); // find number of character in string
console.log(name.toUpperCase()); // it will upper case the name but will not effect name variable
console.log(name.charAt(2)); // it will show that given index which character is 